<%= bootstrap_form_for User.priorities do |c| %>
  <%= c.text_field :text, placeholder: 'priority' %>
  <%= c.primary "Create" %>

  <ul><% @priority.each do |cc|%>
    <div class="well">
     <li><%= cc.text %></li>
    </div>
   <% end %>
  </ul>

<% end %>